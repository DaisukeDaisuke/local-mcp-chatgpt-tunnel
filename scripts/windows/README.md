# Windows scripts
The former all-in-one PowerShell installers and runtime-key storage scripts were removed.
Use the explicit commands in `INSTALL.md` and the single-purpose Node.js scripts under `scripts/node`.
No script in this directory installs packages, stores credentials, changes machine policy, or requires elevation.
